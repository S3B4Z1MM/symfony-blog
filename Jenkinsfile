pipeline {
  agent any
  
  stages {
    stage("build") {
      sh 'echo "Start building ..."'
    }
    
    stage("test") {
      sh 'echo "Start testing ..."'
    }
    
    stage("deploy") {
      sh 'echo "Start deolying ..."'
    }
  }
}
